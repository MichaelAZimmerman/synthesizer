import React from "react";

const About = () => {
  return (
    <>
      <h4 className="about">
        This page is for messing around with synthesizer effects and settings
        using Tone.js. Click "Keyboard" in the menu to begin. It seems to glitch
        out after playing roughly 40 notes.
      </h4>
    </>
  );
};

export default About;
